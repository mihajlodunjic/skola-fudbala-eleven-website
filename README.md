# skola-fudbala-eleven-website
# Škola fudbala ELEVEN

Astro statički sajt spreman za Render Static Site.

Build komanda:

```bash
npm ci && npm run build
```

Publish directory:

```bash
dist
```

Production origin se podešava kroz `PUBLIC_SITE_URL`, bez završne kose crte.
